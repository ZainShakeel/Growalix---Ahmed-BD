{
  "name": "growalix-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Growalix digital agency website — static site generated from src/",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node serve.mjs",
    "start": "node serve.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
